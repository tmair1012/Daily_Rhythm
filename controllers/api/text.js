// post to /api/text from front end for setting up new reminders 
    //from, to, body (or necessary details to build body), time to send, or whether or not recurring

//get to api/text/:id to get texts for user that are still unsent
    //or get all texts (get: /api/text) for all users and send them en masse

// const router = require('express').Router();



// router.get('/api/text', (req, res) => {

// })

